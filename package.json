{
  "name": "cosiri-gmp-platform",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "node -e \"const ua=process.env.npm_config_user_agent||''; if(!ua.startsWith('pnpm/')){console.error('Use pnpm instead'); process.exit(1);}\"",
    "check": "corepack pnpm run typecheck",
    "build": "corepack pnpm run typecheck && corepack pnpm --filter @workspace/api-server build && corepack pnpm --filter @workspace/platform build",
    "start": "corepack pnpm --filter @workspace/api-server start",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm --filter @workspace/platform typecheck && corepack pnpm --filter @workspace/api-server typecheck"
  },
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "4.59.0",
    "@tailwindcss/oxide-win32-x64-msvc": "4.2.1",
    "lightningcss-win32-x64-msvc": "1.31.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
